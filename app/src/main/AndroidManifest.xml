<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bd.org.coronacare">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".login.mobile.LoginWithMobileOTPActivity"></activity>
        <activity android:name=".login.mobile.LoginWithMobileActivity" />
        <activity
            android:name=".login.LoginOptionsActivity"
            android:theme="@style/AppTheme.DarkStatusBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.analysis.AnalysisResultActivity" />
        <activity android:name=".profile.edit.EditProfileActivity" />
        <activity android:name=".emergency.EmergencyActivity" />
        <activity android:name=".about.AboutActivity" />
        <activity android:name=".main.MainActivity" />
    </application>

</manifest>